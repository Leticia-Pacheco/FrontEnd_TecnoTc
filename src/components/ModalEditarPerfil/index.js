import { Container, Header, Overlay } from "./styles";

function EditProfile () {
    return(
        <Overlay>
            <Container>
                <Header>

                </Header>
            </Container>
        </Overlay>
    );
}

export default EditProfile;